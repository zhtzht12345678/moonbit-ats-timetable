# Examples

demo_schedule.xml is the documented lightweight XML exchange subset accepted by schedule_from_xml_text.

The CLI demo uses the richer in-memory synthetic fixture from demo.mbt:

~~~text
moon run --target native cmd/ats-demo -- --action conflicts
moon run --target native cmd/ats-demo -- --action diagram
~~~

